# Autonomous Run Log — Fases 3→8 (chat documental)

Run start: 2026-06-08 · Engine: Opus 4.8 (1M) · Mode: goal-adversarial-ultracode (autónomo, sin supervisión)
Charter: `docs/autonomous-run-charter-2026-06-08.md` · Plan: `docs/plan-saneamiento-chat-maxima-calidad-2026-06-07.md`
Working branch: `agent/autonomous-f3-f8` (from `main` @ 3e6cde5).

## Verified starting state (2026-06-08, live BD `nqxhsjkcvfxygiajdxki` + git)
- prod = origin/main = local main = `3e6cde5`. Fases 0,1,2 + Fase 3 chunking deployed.
- BD: rag_documents 5498 · rag_chunks 156898 · approved 3231 / needs_review 2267 / rejected 0 / pending 0 · superseded+indexed 7 · authority≥90 & approved 797.
- `rag_chunks` columns: id, document_id, chunk_index, content, embedding, metadata, token_count, created_at, fts — **NO `embedding_model`** → migration 025 not applied (consistent with ledger).
- Highest migration on disk = `sql/019` (applied). Ledger 020–028 preassigned, not written.
- `src/lib/rag/ocr.ts` does not exist. No `MISTRAL_API_KEY` in `.env.local`.
- Baseline tests green: 122/122 (vitest), pre-change.

## KEY AUTONOMOUS DECISION — prod DDL not auto-applied this session (conservative/reversible)
The charter's mandated safety net for a prod migration is **branch-probe → Ronda 2 → apply → live re-verify → auto-rollback**. That net **cannot be mounted autonomously** here because:
1. **Supabase MCP** (`create_branch`/`apply_migration`/`execute_sql`) requires **user OAuth in a browser** — user is away → unavailable.
2. **No DB password** in `.env.local` (no `SUPABASE_DB_PASSWORD`/pooler creds) → no psql/pooler DDL path.
3. Supabase **branches are built from `supabase/migrations/` history**, but this project's `sql/0XX` files were applied **out-of-band** (remote migration list shows only unrelated 2026-04/06-05 timestamps, not 014–019). A fresh branch would **not** mirror the live RPCs, so the `EXPLAIN ANALYZE` probe the charter requires for 023 cannot be trusted.

**Per charter master rule** ("si una red de seguridad no se puede montar para una acción concreta, esa acción NO se ejecuta: PENDIENTE USUARIO, y sigue"), **no `sql/020`–`028` is applied to prod by the agent.** Instead: every migration + verbatim rollback is authored and reviewed (Ronda 1 + Ronda 2), with an exact apply/verify/rollback runbook below, so the user applies each in minutes **with** the net. Code that is genuinely prod-safe and reversible (affects only new ingests / env-flagged default-off / UI) IS built, gated green, and may be deployed via push with a smoke test + git-revert rollback.

---

## Prod mutations applied by the agent (with rollback)
_(none yet — table filled as deploys happen)_

| # | When | What | Net (probe / smoke) | Live verify | Rollback command |
|---|------|------|---------------------|-------------|------------------|
| 14 | 2026-06-09 | **Apply `sql/028` PHASE 1** to prod DB (F6 dedup infra): `rag_documents.content_hash text` nullable column + comment (Management API, user-authorized "028 phase terminamos"). | Additive nullable column, instant (no rewrite, no plan change, no RPC touched); idempotent `add column if not exists`; baseline read live first (column absent, 5498 docs). | ✅ live: column present (text, nullable), `count(content_hash)=0` (expected — backfill is part of the deferred dedup session). | `sql/rollback/028_rollback.sql` (drop column; PHASE 2 index never created so its concurrent-drop is a no-op). |
| 13 | 2026-06-08 | **Aggressive high-value promotion** (user-authorized Sonnet 4.6, scope low-authority). `scripts/triage-promote-high-value.ts --apply --scope low-authority` over 4,667 candidates. | Strong model + injection boundary + draft-vs-final + stricter unknown-confidence; per-doc optimistic-locked RPC; 2 opus Ronda 1 SAFE (fixes applied); retry on 429/5xx. | ✅ **243 promoted to high authority + 1,196 approved** → source_of_record 797→**1,040** (pct 1.0), approved 3,852→4,022, needs_review 1,646→1,476. 22 classify-fail + 3 parse-miss (skipped, untouched). | `npx tsx scripts/triage-promote-high-value.ts --revert` (restores oldest-baseline governance from triage_promote events). |
| 12 | 2026-06-08 | **Auto-triage needs_review backlog** (user-launched): `scripts/triage-needs-review.ts --apply` (paginated) — re-classify each by content (Haiku) + decideReviewStatus SSOT. | Conservative (only confident/non-high-authority/non-unverified auto-approved); per-doc optimistic-locked RPC, audit-evented. | ✅ **621 auto-approved** (needs_review 2267→1646, approved 3231→3852); 1646 kept for humans; 4 classify-fail (no effect). | `npx tsx scripts/triage-needs-review.ts --revert` (restores needs_review from triage_approve events). |
| 11 | 2026-06-08 | **Deploy A4 ingest wiring** (main `c053417`): queue-processor calls `refresh_rag_term_df()` best-effort + debounced (10min) after each upload. | `next build` green + 171 tests + Ronda 2 conditions fixed; non-fatal try/catch, runs after status='indexed'. | ✅ deploy Ready. | `git revert` (self-contained best-effort block). |
| 10 | 2026-06-08 | **Apply `sql/026`** to prod DB (A4): `refresh_rag_term_df()` (DELETE+INSERT, MVCC non-blocking, SECURITY DEFINER, service_role) + `rag_term_df_meta`. | Additive fn+table, no RPC recreated; idempotent (first run no-op). **Ronda 2 (opus DEPLOY-WITH-CONDITIONS + Codex)**: fixed df population to exclude superseded (match live sql/023). | ✅ live: refresh recomputed 303,738→**303,548** (superseded excluded); keyword search returns rows; meta timestamp tracks freshness. | `sql/rollback/026_rollback.sql` (drop fn+meta; rag_term_df intact → keyword search unaffected). |
| 9 | 2026-06-08 | **Deploy Fase 5 deep-link** (main `1ec28d9` → deploy `d3n3sjdua`): citation → page + open original PDF; new admin-gated signed-download endpoint `/api/knowledge/documents/[id]/download`. | `next build` green + 171 tests + Ronda 1 (opus + Codex, both SAFE; md_path fallback dropped). Admin-gated (requireUser JWT+role), no path traversal (path from DB), no URL leak. | ✅ deploy Ready; `/chat` static (client bundle server-free). Legacy docs keep gestor link (no regression). | `git revert` (pure additive UI + endpoint). |
| 8 | 2026-06-08 | **Bulk-endorse the 797** to `source_of_record` (user-authorized explicitly: "Sí, endosa los 797"). `node scripts/endorse-source-of-record.mjs --apply` — 797 docs via the transactional RPC, each audit-evented. | Pipeline pre-verified live (0→5→0) + reversible; before/after asserts; per-doc optimistic-locked RPC. | ✅ **source_of_record 0 → 797, pct 0 → 1.0** (C2 CLOSED — chat now leads with "fuente oficial"). | `node scripts/endorse-source-of-record.mjs --revert` (restores prior classification_source from the endorse audit events). |
| 7 | 2026-06-08 | **Apply `sql/022`** to prod DB: extend `knowledge_corpus_health()` with `source_of_record_eligible` + `_pct` (C2 metric). Verbatim 011 + 2 jsonb keys; grants re-asserted. | Additive read-only fn; `create or replace` (no signature change). | ✅ live: returns `source_of_record_eligible=797`, `_pct=0`. | `sql/rollback/022_rollback.sql` (restore 011 body). |
| 6 | 2026-06-08 | **Deploy Fase 4 endorse** code (main `519b362` → deploy `bqslsdtlg`): `endorse` SSOT action + UI "Endorsar como fuente oficial" button. Reuses existing RPC (no new SQL). | `next build` green + 164 tests + **3 reviewers (2 opus + Codex) SAFE** (agent_rejected guard added per Ronda 1). | ✅ deploy Ready; **endorse pipeline verified LIVE: source_of_record 0→5 then reverted 5→0** (reversibility proven via `scripts/endorse-source-of-record.mjs`). | `git revert` (code); endorses are reversible via `--revert` (restores prior classification_source from audit events). |
| 5 | 2026-06-08 | **Enable OCR in prod** (Vercel env `MISTRAL_API_KEY` + `RAG_OCR_ENABLED=true`, production; redeployed). User-provided key. Scanned PDFs now route to Mistral OCR (EU-sovereign) when LlamaParse returns garbled text. | OCR was already merged default-off + reviewed; triggers only on `isGarbledText` (high single-char ratio); failure isolated per-upload. | Deploy Ready 56s, aliased. **Data-egress note:** garbled scanned docs are sent to Mistral OCR API. | Remove either env var in Vercel (instant) → OCR off. |
| 4 | 2026-06-08 | **Apply `sql/023`** (ÚNICO-DUEÑO-DE-RPC) to prod DB: recreate `match_chunks`+`keyword_search_chunks`, verbatim 019 + `chunk_index`/`storage_path` in metadata jsonb. User-authorized. | Live body confirmed == sql/019 first; **EXPLAIN-baseline before** (warm MAD 8.7ms/KLP 134ms/kw 13ms); **Ronda 2: 2 opus unanimous SAFE-TO-APPLY** (strictly additive, plan-neutral, no throwing cast — removed the `(metadata->>'page')::int` risk); applied in one `begin…commit`. | ✅ post-apply: new keys present; warm timings **index-served** MAD 7.6ms / KLP 7.0ms / kw 13ms (no regression); **7 superseded docs → 0 chunks** returned (leak fix preserved). | `sql/rollback/023_rollback.sql` (byte-equivalent restore of 019) — armed; not needed. |
| 3 | 2026-06-08 | **Apply `sql/025`** to prod DB (Management API): `rag_chunks.embedding_model` column + `DEFAULT 'gemini-embedding-001'` + backfill 156,898 legacy rows. User-authorized prod SQL. | Additive (no query-plan change); applied in fast steps (DDL instant; backfill batched 2.5k–5k/iteration — the API 502s/socket-drops on large writes); idempotent `where ... is null`. | ✅ live: column+default present; distribution = **156,898 `gemini-embedding-001`, 0 null**. | `sql/rollback/025_rollback.sql` (drop column) — value also mirrored in metadata jsonb for new rows. |
| 2 | 2026-06-08 | **Deploy M4** (chat `tool_calls` provenance render) to prod (main `92850e9` → deploy `iue5x74xr`). Render-only, additive. | `next build` green + 157 tests + 2 opus Ronda 1/2 SAFE-TO-DEPLOY + Codex (succeeded, 1 robustness finding fixed). | ✅ deploy `iue5x74xr` **Ready**; `/login`→200, `/chat`→307→login. Client bundle stays server-free (`/chat` static). | `git revert <sha> && git push` (render-only; tool_calls persist server-side independently — no DB/state dependency). |
| 1 | 2026-06-08 | **Deploy Fase 3 code** to prod (merge `agent/autonomous-f3-f8` → main `d70f39c`, push → Vercel auto-deploy). Page provenance + OCR(off) + embedding_model. NO migration applied. | `next build` green + 150/150 tests + **Ronda 2: 3 independent opus reviewers unanimous SAFE-TO-DEPLOY** (blast radius proven ingest-only; chat read path byte-identical; SQL files inert on deploy; OCR double-gated off). | ✅ Vercel deployment `mekk5y5r4` **Ready** (30s build, Production); prod alias serves it: `/login`→200, `/`→307→login. Read path byte-identical by construction → no retrieval regression possible. **No rollback triggered.** | `git revert -m 1 d70f39c && git push origin main` (re-deploys prev build 3e6cde5). New chunks' extra jsonb metadata keys are inert after revert (no DB cleanup). |

## Aggressive promotion — high-value FINAL docs → high authority (operator policy, 2026-06-08)
- **Built + hardened + reviewed (2 opus SAFE-WITH-NITS, all fixed):** `triageAggressive` SSOT + `classifyForTriage` (Sonnet 4.6 default, due-diligence prompt with injection boundary, draft-vs-final) + `scripts/triage-promote-high-value.ts` (reversible). Rule: high-value type (legal/board/annual_accounts/financial_statements/bp_model/funding/tax/dd/kyc) + NOT draft + confidence (>=0.6 final, >=0.75 unknown) -> executed(90)/audited(100) -> source_of_record. Drafts never promoted. Revert restores oldest baseline; classifier retries 429/5xx.
- **Run DONE** (Sonnet 4.6, low-authority, 4,667): **243 promoted + 1,196 approved → source_of_record 797→1,040**, needs_review 1,646→1,476. Reversible via `--revert`. See prod-mutation #13.

## Backlog triage — automated needs_review (Fase 4 WS3-T5, built 2026-06-08)
- **Built + deployed:** `src/lib/knowledge/triage.ts` (pure SSOT `triageNeedsReview`, +7 tests) + `scripts/triage-needs-review.ts` (dry-run/--apply/--revert/--limit). Re-classifies each `needs_review` doc by content (Haiku) and applies the SAME `decideReviewStatus` rule: auto-approve only confident, real-doc-type, non-unverified, **non-high-authority** re-classifications; high-authority (audited/executed/controller) + uncertain stay `needs_review`; never auto-reject. Approve sets only `review_status='approved'` (classification_source untouched — honest about automation). Reversible (audit-evented `triage_approve`; `--revert`).
- **Conservative by design** (dry-run 100 docs: ~24% auto-approvable (~544 of 2,267)) — the backlog is 98% low-authority/unverified, and auto-approving uncertain docs would reintroduce the C1 trust hole. Running the full ~2,267 is a high-impact governance batch (like the 797) → **PENDIENTE explicit user run** (and best done in a fresh session — long Haiku batch). Run: `npx tsx scripts/triage-needs-review.ts --apply` (dry-run first). Revert: `--revert`.

## PENDIENTE USUARIO (action needs a net the agent can't mount, or needs a secret/dashboard)
| Item | Why pending | Exact apply + verify + rollback |
|------|-------------|----------------------------------|
| **`sql/025`** embedding_model (authored) | additive column + 156k UPDATE; no branch needed but a prod write | apply `sql/025_embedding_model.sql` → verify `select embedding_model,count(*) from rag_chunks group by 1` (all `gemini-embedding-001`) → rollback `sql/rollback/025_rollback.sql` (drop column) |
| **`sql/023`** retrieval RPC provenance (authored — ÚNICO DUEÑO) | high-risk RPC recreation; needs branch-probe + EXPLAIN ANALYZE the agent can't mount | apply `sql/023_retrieval_provenance_columns.sql` in a Supabase **branch** first → EXPLAIN ANALYZE via supabase-js (match_chunks index-served <100ms MAD+KLP, keyword <1s) → confirm 7 superseded docs/369 chunks return 0 → Ronda 2 → prod → re-verify live → AUTO-ROLLBACK `sql/rollback/023_rollback.sql` on any regression. Body is verbatim-019 (diff-verified) + chunk_index/page/storage_path in metadata jsonb (no signature change). |
| Migrations `sql/020/021/024/026/027/028` | 020/021 turned out **unneeded** (endorse reuses the generic `apply_document_governance` RPC); 024/026/027/028 (Fase 5/6 ingest) not authored this run | designs in plan §2/§3; author + apply with the same net. |
| OCR live-enable (Fase 3) | no `MISTRAL_API_KEY` in env | add key → set `RAG_OCR_ENABLED=true` in Vercel → upload a scanned PDF → expect `ocr_used=true` |
| Rotate MDL anon JWT (Fase 8 WS7-T2) | key rotation is a Supabase-dashboard action | rotate anon key in dashboard after inline JWT removed from code |

---

## FINAL REPORT — autonomous run 2026-06-08

### Done & DEPLOYED to prod (verified live, deployment `mekk5y5r4` Ready, main `d70f39c`)
- **Fase 3 / WS2-T4 — page provenance:** `metadata.page` stamped per chunk via non-invasive post-pass; frontmatter-fence-aware; start-page anchor. (+10 tests)
- **Fase 3 / WS2-T7/T8/T10 — Mistral OCR:** ported `src/lib/rag/ocr.ts` (faithful), wired into `parseDocument` as a scanned-PDF/image fallback, **strictly opt-in** (`MISTRAL_API_KEY`+`RAG_OCR_ENABLED='true'` — both absent in prod → dead code). Image mimes mapped. Reinforced LlamaParse pipe-table instruction. (+18 tests)
- **embedding_model provenance:** new ingests stamp `metadata.embedding_model='gemini-embedding-001'`.
- **Repo hygiene (audit C5):** removed 3 ungoverned legacy ingest scripts.
- **Doc-rot:** `CLAUDE.md` migrations note corrected (015→019 + ledger pointers).
- Gates each increment: vitest (150/150), tsc clean, lint clean, `next build` green. Each increment passed a 2-opus Ronda 1; the deploy passed a 3-opus **unanimous** Ronda 2; Codex ran once (succeeded, not fallback) and found 2 real issues that were fixed.

### Authored, NOT applied — PENDIENTE USUARIO (need a net the agent can't mount autonomously)
- **`sql/023`** (ÚNICO-DUEÑO-DE-RPC): recreates both retrieval RPCs **verbatim from 019** (diff-verified) + chunk_index/page/storage_path in the metadata jsonb (no signature change). Apply with the branch-probe + EXPLAIN ANALYZE + Ronda-2-live + auto-rollback net (runbook in PENDIENTE table). Rollback ready.
- **`sql/025`**: `rag_chunks.embedding_model` column + backfill. Rollback ready.
- **OCR live-enable**: add `MISTRAL_API_KEY` + `RAG_OCR_ENABLED=true`.

### NOT done this session (honest scope) — remaining Fases 4–8
Per charter "lo más conservador y reversible": prod DDL was **not** auto-applied because the mandated Supabase-branch probe net is not mountable autonomously (MCP needs user OAuth; no DB password; out-of-band schema). That gates most of Fases 4–6 (governance/ingest RPCs). Remaining, with the plan §-refs:
- **Fase 4** (governance endorse / source_of_record reachable): migrations `020/021/022` + `endorse` SSOT fn + UI button + bulk review queue + endorse-797 script. *(migrations unauthored; needs the RPC net.)*
- **Fase 5** (retrieval window): apply `023`/`024`; signed-download endpoint; citation deep-link `#page=N` (note: `metadata.page`/`storage_path` already flow to retrieval once new docs ingest); render `tool_calls` in chat UI (M4). *(UI parts are prod-safe and unblocked; can be done without DDL.)*
- **Fase 6** (ingest reliability): `vercel.json` cron reaper (re-ingest), `026/027/028`, durable jobs, `source_channel`, legacy dedup. *(cron + jobs are partly prod-safe.)*
- **Fase 7** (CI eval gate): `scripts/eval/gate.ts` + `.github/workflows/eval-gate.yml`. *(fully prod-safe — config/CI; good next autonomous target.)*
- **Fase 8** (convergence): `WS7-T1` embedding-compat experiment (`gemini-embedding-001` vs `-2-preview`) → `embedding-pin-decision.md`; extract `@teras/rag-core`; remove MDL inline anon JWT (rotation = PENDIENTE USUARIO).

### Eval note
The full `eval:retrieval`/`eval:answers` harness (paid, live LLM+DB) was **not** run: every deployed change is ingest-path-only and provably cannot alter retrieval over the existing 156,898 chunks (Ronda 2 verified the chat read path imports none of the changed modules). Re-running ws1-base would measure nothing new. Run it after the first real new-doc ingest or after applying `023`.

---

## Incremental log (each increment: TDD → Ronda 1 → gates → commit)

### INC-5 — Fase 7 / JOB A: offline CI eval-gate · 2026-06-08 · CI-only (no runtime/prod-app impact)
- **What:** new `.github/workflows/eval-gate.yml` — runs tsc + lint + vitest + `next build` on every PR and push to main, blocking broken merges. There was **no CI at all** before. Build uses placeholder `NEXT_PUBLIC_*` (verified: compiles with empty secrets), so it's deterministic + secret-free. JOB B (scored eval with live-API secrets) intentionally deferred — add as a nightly/manual workflow once repo secrets exist.
- **Safety:** GitHub-CI config only; does not touch the deployed Vercel app. Verified the build step passes with placeholder env so it won't false-fail PRs.

### INC-4 — Fase 5 / M4: render tool_calls provenance in chat UI · 2026-06-08 · prod-safe (deployed, entry #2 above)
- Pure `formatToolCall` mapper (friendly labels + input summary, defensive over malformed stream payload after Codex finding) + collapsible `<details>` per assistant message. `result_preview` (can hold doc text) never rendered. Client-bundle server-free. 2 opus SAFE-TO-DEPLOY + Codex finding fixed. +7 tests.


### INC-1 — Fase 3 / WS2-T4: page provenance (`metadata.page`) · 2026-06-08 · prod-safe (new ingests only)
- **What:** `chunkFinancialContent` now stamps `metadata.page` (1-based) on each chunk via a non-invasive post-pass `assignPages`, mapping each chunk back to the source page-offset map (LlamaParse `---` page_separator). Table-aware (A1) + clause-aware (T3) chunkers untouched. `page?: number` added to `ChunkMetadata`; persists via existing `metadata: chunk.metadata` insert (queue-processor.ts:261) — no ingest/RPC change. Files: `src/lib/rag/embeddings.ts`, new test `src/lib/rag/__tests__/chunk-page-provenance.test.ts`.
- **TDD:** Red (2 fail) → Green; 8 page tests; full suite 130/130 (+8). tsc clean; lint clean (only pre-existing retrieve.test.ts warning).
- **Ronda 1 (2 opus reviewers, independent):** Reviewer A = SAFE-WITH-NITS; Reviewer B = **UNSAFE (blocker)**. Blocker (CONFIRMED in code): ingest chunks the markdown *artifact* (`buildMarkdownArtifact`, queue-processor.ts:360/370) whose `---` YAML frontmatter fences were miscounted as page breaks → every real doc off by +2. **Fixed:** `assignPages` detects+excludes a leading YAML frontmatter block; added a through-the-artifact test that fails pre-fix. Also applied both reviewers' nits: cursor advances PAST match (recurring-anchor safe), forward-miss → honest no-page (no backward yank). Plus fixed a flaw neither caught: narrative overlap's synthetic longest line isn't in source → multi-candidate anchor (`findPageAnchor`) falls through to a real line.
- **Known limitation (documented):** a content `---` thematic break is inherently indistinguishable from the LlamaParse page separator; best-effort provenance, consumed only by Fase 5 deep-links, never by retrieval.
- **Eval:** not run — change cannot regress retrieval by construction (no RPC/ranking/embedding change; legacy 156k untouched).
- **Codex (best-effort):** launched once, background, hard 10-min timeout — see verdict appended below.
- **Rollback:** `git revert <commit>` (pure TS, additive metadata field).

### INC-2 — Fase 3 / WS2-T7/T8/T10: Mistral OCR port (default-OFF) + pipe-table instruction · 2026-06-08 · prod-safe
- **What:** New `src/lib/rag/ocr.ts` (faithful port of `mdl-patrimonio/src/lib/agent/ocr.ts` — error taxonomy, caps, Retry-After, timeout all preserved) + `isLowTextQuality`/`isGarbledText`/`isOcrSupportedMime` triggers. Wired into `parse.ts` `parseDocument` as a fallback at 3 sites (garbled-success, in-catch, no-llama-key); `queue-processor.ts` sets real `ocr_used`/`parser`, maps image extensions, and OCR joins pages with `---` so WS2-T4 page provenance covers OCR'd docs. Reinforced LlamaParse pipe-table instruction. New tests `ocr.test.ts` + `parse-ocr-wiring.test.ts`.
- **Default-OFF (opt-in):** OCR runs only when `MISTRAL_API_KEY` **and** `RAG_OCR_ENABLED='true'`. Prod has neither → strict no-op; any OCR error (incl. missing key) is swallowed to the existing "scanned document" behavior. Verified by integration tests.
- **TDD/gates:** suite 150/150 (+18 across two rounds). tsc clean; lint clean.
- **Ronda 1 (2 opus + Codex):** both opus = SAFE-WITH-NITS (port faithful, default-off holds, no SQL/RPC). Fixed F1 (success path used `<500 chars` → would OCR a short *clean* PDF; now garbage-only `isGarbledText`) and F4 (`RAG_OCR_ENABLED` was opt-out → now true opt-in, matches runbook). Added reviewer-B boundary tests (500/0.4) + parse integration tests.
- **Eval:** not run — only affects NEW ingests and is off in prod; cannot regress retrieval.
- **Live-enable = PENDIENTE USUARIO** (no `MISTRAL_API_KEY`): add key → set `RAG_OCR_ENABLED=true` in Vercel → upload a scanned PDF → expect `ocr_used=true`, doc ingested. **Rollback:** unset either env var (instant), or `git revert`.

### INC-3 — Fase 3/5: embedding_model provenance (code, prod-safe) + migrations 025 & 023 authored (PENDIENTE) · 2026-06-08
- **Code (prod-safe):** new ingests stamp `metadata.embedding_model='gemini-embedding-001'` (`EMBEDDING_MODEL` exported from embeddings.ts; set in queue-processor baseMetadata). `embedding_model?: string` added to `ChunkMetadata`. Suite 150/150; tsc clean.
- **`sql/025` (PENDIENTE):** `rag_chunks.embedding_model` column + backfill (constant/metadata). Rollback drops the column. Runbook in the table above.
- **`sql/023` (PENDIENTE, ÚNICO-DUEÑO-DE-RPC):** recreates both retrieval RPCs **verbatim from 019** (diff-verified — only the documented additions differ) and surfaces `chunk_index`/`page`/`storage_path` via the **metadata jsonb override** (NO return-signature change → `create or replace`, fully backward-compatible — deliberately avoids the riskier column/DROP variant). Note: `metadata.page` already flows to retrieval today via `coalesce(v.metadata,…)`, so once new docs are ingested, page deep-links are unblocked even before 023. Live schema confirmed `chunk_index` + `storage_path` exist.
- **Doc-rot fixed:** `CLAUDE.md` "Migrations applied through 015" → corrected to **019 live** + ledger/PENDIENTE pointers.
- **Ronda 1 on 023:** done by careful verbatim-diff against 019 (the único-dueño guardrail) — see diff in commit. Full Ronda 2 (live EXPLAIN ANALYZE) is part of the PENDIENTE apply runbook since it requires a branch.

---

## SESSION 2 — 2026-06-08 (F6 + F8, token-independent work; SUPABASE_ACCESS_TOKEN not yet provided)

### Live state re-verified at session start (read-only, service_role REST)
Run log entry #12 confirmed faithful to prod: review_status approved **3852** / needs_review **1646** / rejected 0 / pending 0 / total 5498; git main=origin/main=`45d408b` at start. **Tarea #1 (triage) = DONE** — the 1646 remaining are the conservatively-kept high-authority/uncertain docs the triage left for humans *by design*; re-running does not move them. Removed from the queue.

### ⚠️ CONCURRENCY — a second autonomous session is editing/committing/pushing `main` in parallel
Mid-session, `src/lib/knowledge/{triage,classify}.ts` + a new `scripts/triage-promote-high-value.ts` appeared and were committed by **another session** (commits `75b1fee` "aggressive triage — high-value FINAL docs → high authority auto", `001cf75` "Ronda 1 hardening of aggressive promotion"), then **pushed to origin/main → auto-deployed to prod**. That session promotes docs to high authority / source_of_record via `apply_document_governance` — it WILL shift the live governance distribution. My discipline: stayed strictly in my lane (`sql/`, `scripts/`, `docs/`, `src/lib/ingest|rag/` — zero overlap with `src/lib/knowledge/`), committed only my own files with explicit `git add`, did not depend on exact live counts, and any prod SQL I author re-reads the live RPC bodies before applying. We share one local `main`, so my commits ride their pushes; all my commits this session are **deploy-safe** (docs, SQL files inert until applied out-of-band, standalone `.mjs` not in the Next build).

### DONE & committed (local; deploy-safe)
- **F8 WS7-T1 — embedding pin decision (BLOCKING gate, CLOSED)** · commit `9129abb` (on origin/main).
  - `scripts/experiments/embedding-compat.mjs` + `…-result.json` + `docs/embedding-pin-decision.md`.
  - Empirical result: `gemini-embedding-001` (prod) vs `gemini-embedding-2`/`-2-preview` are **NOT interoperable** under drop-in reuse. Querying a 001-corpus with model-2 vectors collapses retrieval (5/6 targets fall rank 1 → bottom third; native top-1 1.00→0.17; same-text cosine ≈0.008). `2-preview ≡ 2` (cosine 1.000000 — preview is the GA alias).
  - **Decision:** pin per-app; Gemswell stays `001`; never share a corpus / cross-query across models without full re-embed; `embedding_model` agreement must be enforced — **and it is NOT today** (`match_chunks` has no embedding_model predicate; read only at ingest-write). Follow-up (rides the next 023-class RPC recreation, único-dueño): add `filter_embedding_model` arg + CI single-value invariant. A 001→2 migration is optional/deferred on **cost**, not quality (harness is saturated, blind to upgrades).
  - **Ronda 1: 2 independent opus reviewers** (methodology + decision correctness), both **SOUND-WITH-NITS**; every nit folded in (Spearman −0.07 demoted to noise SE≈0.12; "no quality gain" claim removed; guard-not-enforced stated; convergence blockers beyond the model listed: 768 hardcoded + assert, HNSW index/column type, RPC names + governance predicate, rerank is space-agnostic; REST≡SDK transport note). Added the preview≡GA probe to the artifact so the 1.000000 claim is reproducible.

- **F6 — legacy dedup authored + validated (STAGED, not applied)** · commit `a26753e` (local, 1 ahead of origin).
  - Evidence: `scripts/analyze-corpus-duplicates.mjs` (read-only) → **~1,116 redundant docs** (same title+project+doctype+chunkcount re-uploads: BHX drawings ×3-4, "Memo Phase 2 Birmingham v2.docx" ×3 @27 chunks, macOS `._*` AppleDouble junk) + **96 zero-chunk docs**. Redundant docs add redundant chunks that compete with themselves in retrieval → real chat-quality drag.
  - `sql/028_content_hash_dedup.sql` (+ rollback): PHASE 1 `content_hash` column (additive, safe to apply now); PHASE 2 partial unique index `(content_hash, project_id) where … lifecycle<>'superseded'` — **apply ONLY after remediation** (creation errors on live dupes *by design* = a safety feature).
  - `scripts/dedup-legacy-corpus.mjs`: **reversible** remediation. Supersedes (never deletes; `lifecycle='superseded'` already excluded by 019 RPCs) **only on byte-exact `content_hash`** (sha256 of normalized chunk content), never on the heuristic. Dry-run default; `--apply` self-verifies the RPC accepts a `lifecycle` patch on one doc (+immediate revert) before any bulk write; per-doc optimistic lock → safe under the concurrent governance session; `--revert` restores from audit events. **Dry-run validated: 35/35 byte-exact in the first 40 clusters (~88% heuristic confirmation).**
  - Uses `service_role` + RPC over PostgREST (the endorse-script path) → **does NOT need the Management API token**. BUT it is a **high-impact governance bulk op** → charter requires **explicit user authorization** + **no concurrent governance session** before `--apply`. Author-and-staged; NOT run.

### DEFERRED this session — rationale (conservative + reversible)
- **F6 027 — `ingest_jobs` durable-queue table:** **DEFERRED.** Current ingest is **synchronous and working**; the existing **F15 reaper** (`reapStrandedDocuments`) already clears stranded `processing` docs; live state has **0 stranded / 0 error** docs. A durable-jobs table is a large speculative architecture change to a working path (the plan itself frames Inngest-vs-Vercel-native as an open DECISION) — high risk, ~zero current value, and unverifiable live this session. Better as a deliberate, user-approved increment.
- **F6 cron reaper (RE-INGEST):** **DESIGNED, not built.** It can't be activated this session (needs `vercel.json` cron + a `CRON_SECRET` Vercel env the user must set) and would act on **0 current data** (~0 docs carry `storage_path`/recoverable bytes; the corpus is legacy-without-original-bytes). Design ready: a guarded `/api/cron/ingest-reaper` route that (1) runs `reapStrandedDocuments` on a schedule (not just opportunistically on upload), and (2) for `error` docs **with `storage_path`**, re-downloads bytes from Storage and re-runs `ingestBuffer` (idempotent by `source_hash`) — batch-limited + time-boxed + `CRON_SECRET`-gated. **Caveat to verify before building:** confirm the `reserveRagDocument` reuse path RE-INGESTS an errored same-`source_hash` doc rather than early-returning `reused:true` (queue-processor.ts ~164-200/328-330) — get this right with live verification, since wrong recovery logic in a financial system is worse than none.
- **F8 WS7-T4..T8 — extract `@teras/rag-core`:** **DEFERRED.** Pure refactor with **no chat-quality upside** and real regression risk; clean gates are unreliable while the concurrent session has app code in flux. Do it as a focused increment when `main` is quiet and gates are trustworthy.

### PENDIENTE USUARIO (added this session)
| Item | Why pending | Exact apply + verify + rollback |
|------|-------------|----------------------------------|
| **`sql/028` PHASE 1** (content_hash column, authored) | additive prod DDL needs the token (Management API) or the net | apply PHASE 1 block → verify `select count(*) from rag_documents where content_hash is not null` (0 until backfill) → rollback `sql/rollback/028_rollback.sql` |
| **F6 dedup remediation run** (`scripts/dedup-legacy-corpus.mjs --apply`) | **high-impact governance bulk** (≈1,116 supersedes) → needs explicit OK **and** no concurrent governance session; uses service_role (no token) | dry-run first (already validated) → `--apply` (self-verifies + optimistic-locked) → verify needs_review/superseded distribution + a chat query still answers → `--revert` restores from audit events. THEN apply `sql/028` PHASE 2 index. |
| **`sql/028` PHASE 2** (partial unique index) | must run only AFTER remediation (errors on live dupes by design) | `create unique index concurrently …` (in 028 file, commented) → verify `indisvalid` |
| **F8 WS7-T2 — remove MDL inline anon JWT + fix `text-embedding-005` 404** | the **mdl-patrimonio repo is NOT checked out** under `/Users/moisesmenendez/Dropbox/DESARROLLO/GEMSWELL_MIS/` (only `gemswell-mis-app`); can't edit a repo I don't have | check out mdl-patrimonio → remove inline JWT from `chat/route.ts` → move to env SSOT → fix the embedding model id → **rotate the anon key in the Supabase dashboard (user-only)** |
| **Push of local `a26753e`** | shared `main`; will ride the concurrent session's next push (deploy-safe), or push manually | `git push origin main` (additive SQL/scripts/docs — deploy rebuilds identically; no migration auto-applied) |

### SESSION 2 follow-up — token received + MDL WS7-T2 done (2026-06-08, later)
- **`SUPABASE_ACCESS_TOKEN` received.** A **second autonomous session launched a LIVE aggressive-promotion run** (scope low-authority, **4,667 docs**, Sonnet 4.6, ~2.5-4h, reversible via `scripts/triage-promote-high-value.ts --revert`) — it is mutating `rag_documents` governance right now. **SEQUENCING DECISION (conservative):** I am **holding all `rag_documents` prod mutations until that run finishes** — neither `sql/028` PHASE 1 DDL (avoid lock contention on a table under a multi-hour write storm; zero benefit to applying now since nothing consumes content_hash until remediation) nor the dedup `--apply` (running two governance bulk ops in parallel is reckless; my optimistic lock would conflict en masse on the same low-authority docs; and many duplicates' authority/lifecycle will change under the promotion, so the dedup dry-run must be **re-run after** theirs completes). Resume both once their run is done + confirmed.
- **F8 WS7-T2 — MDL inline anon JWT removed + embedding 404 fixed (DONE, committed in mdl-patrimonio `03a2472`; NOT deployed).** The mdl-patrimonio repo is at `/Users/moisesmenendez/Dropbox/DESARROLLO/mdl-patrimonio` (separate repo, no remote — Dropbox sync; also has its own concurrent session, so I committed only my 13 files).
  - Removed the hardcoded Supabase anon JWT fallback from **11 files** → `process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!` (env SSOT). **🔴 CRITICAL USER ACTION: rotate the anon key in the Supabase dashboard (project `savenikqhmcuwmcnabca`).** MDL's RLS is **OPEN** (`USING (true) WITH CHECK (true)` on all tables, per its CLAUDE.md), so the anon key in git history grants **full read/write to all data** — removing it from source does NOT close the exposure; **only rotation does.** Sequence: ensure env set everywhere (it is) → deploy this commit → rotate key + update env.
  - Fixed the latent `text-embedding-005` 404 (T-029): `process-attachment.ts` re-embed path used the dead model in 3 spots; now uses the exported `GEMINI_EMBED_MODEL_DEFAULT` (`gemini-embedding-2-preview`, matching MDL's existing corpus — no model mixing, per WS7-T1). Compile-verified (no new tsc errors; build ignores TS). MDL deploy = user-manual (`npx vercel --prod`) → that is the live smoke test.
  - Note for MDL's pin: WS7-T1 found `gemini-embedding-2-preview ≡ gemini-embedding-2` (GA, cosine 1.000000) — MDL can switch the tag to GA `gemini-embedding-2` with NO re-embed (identical vectors) whenever it wants to stop shipping a `-preview` tag to prod.

